using UnityEngine;

namespace ClickerEngine
{
    
    //TODO: implement interface IView added method "void Bind(IService service)" this method connects the view and the class that handles business logic.
    // and implement IService as interface for tagging service class (service class - is a class in which business logic is described)
    public class LoadService
    {
        
    }
}
